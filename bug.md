## TodoHead
1. 持续点击确认按钮，第二次之后会弹出toast

## TodoBody
1. 因为内容和备注是用的同一个编辑设置，不能区分开，所以备注也同内容一样不能为空 ====》 已解决
2. 解决了上述问题的同时，有新问题——备注栏一旦为空，就不能重新唤醒输入框

2.  antd如何更改每个数据格子单独的样式

## 难点解决：
1. TodoBody的各个任务不是单独存在的，所以难以筛选。要整体传送筛选后的数据，所以分三种情况。

2.  为todobody创建了数据层，用来给todobody传递数据，但是原本在body里面的action无法通过父传子的传递方式调用，怎么解决？
    答：数据层（容器层）中connect连接的组件就是body自己，而不是容器层组件